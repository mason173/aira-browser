# Third-Party Notices

Runtime dependencies are declared in `package-lock.json` and installed from the npm registry. The direct runtime dependency `better-sqlite3` is distributed under the MIT License. Its transitive dependencies retain their respective license notices in installed packages and source distributions.

This notice is informational and does not replace the license text shipped by each dependency.
